<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.25.0/themes/prism.css"/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.25.0/prism.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <title>{% block title %}PasteTest{% endblock %}</title>

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascript %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>
    <header>
        {% block header %}
            <div class="navbar">
                <a href="{{ path('app_index_page') }}">Главная</a>
                <a href="{{ path('create_paste') }}">Создать пасту</a>
                {% if app.user %}
                    <a href="{{ path('app_personal_account') }}">Аккаунт</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="right">Вход</a>
                {% endif %}
            </div>    
        {% endblock %}
    </header>

    <main style="flex: 1;"> {% block body %} {% endblock %} </main> <!-- Обёртка для основного контента -->

    <footer>
        <p>&copy; 2024 PasteTest. Все права защищены.</p>
    </footer>
</body>
</html>
